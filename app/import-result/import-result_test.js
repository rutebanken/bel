'use strict';

describe('bel.import-result module', function() {

  beforeEach(module('bel.import-result'));

  describe('import-result controller', function() {

    it('should ....', inject(function($controller) {
      //spec body
      var importResultCtrl = $controller('ImportResultCtrl');
      expect(importResultCtrl).toBeDefined();
    }));

  });
});
